# Raw pulls contain customer PII (names, emails, phone numbers). Never commit them.
raw/*.json

# Intermediate build artifacts, regenerated by every build run. Not needed to ship.
*.partial.json
calls.partial.json.bak

node_modules/
__pycache__/

